/**
 * Type definitions for airules configuration
 */

/**
 * Type definition for airules configuration object
 */
export interface AirulesConfig {
  /**
   * Path to directory containing rule files
   */
  rulesDir: string;
}
